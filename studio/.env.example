# API base for the front end. Empty = same origin.
# Local dev defaults to http://localhost:4000 when unset.
VITE_API_BASE=

# Server
PORT=4000
FFMPEG_PATH=ffmpeg
FFPROBE_PATH=ffprobe

# Optional TURN for guests behind strict NAT (STUN-only otherwise)
TURN_URL=
TURN_USER=
TURN_PASS=
